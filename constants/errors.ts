export enum DateFormatErrors {
    NotDefined = "Date is required.",
    NotString = "Wrong date type. The date should be a string.",
    WrongFormat = "Wrong date format. Please use MM/DD/YYYY format."
}

export enum TimezoneFormatErrors {
    NotString = "Wrong timezone type. The timezone should be a string.",
    WrongFormat = "Wrong timezone format."
}
